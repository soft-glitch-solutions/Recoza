enum UserType {
  resident,
  collector,
  admin,
}

enum PickupStatus {
  pending,
  scheduled,
  inProgress,
  completed,
  cancelled,
}

enum WasteType {
  plastic,
  paper,
  glass,
  metal,
  ewaste,
  other,
}

enum CollectorStatus {
  pending,
  approved,
  active,
  suspended,
  inactive,
}